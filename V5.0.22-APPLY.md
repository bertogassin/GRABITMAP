# GRABIT v5.0.22 authoritative Health Connect fix

- Browser/PWA step pages are read-only and never start motion counting.
- Android Health Connect is the only accepted step update source.
- Health Connect may correct a previously inflated value downward.
- Existing account session and Rust `/api/steps` storage remain in use.
